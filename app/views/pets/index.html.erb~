<% if current_user %>
    <%= link_to 'Subir animal',  new_pet_path%>
<% else %>

<% end %>


<header>
  <h1>
  Animales disponibles
</h1>
  Recuerda que para adoptar o subir algun animal para dar en adopcion, debes estar logueado con alguna cuenta.
</header>

<%= form_tag(pets_path, :method => "get", id: "search-form") do %>
    <%= text_field_tag :search, params[:search], placeholder: "Search Pets" %>
    <%= submit_tag "Search"%>
<%end%>

<% @pets.each do |pet|%>
	<h1><%= link_to pet.name, pet_path(pet)%></h1>
		<div><%= pet.kind%></div>
    <div><%= pet.description%></div>
<%end%>
<br>
<%= link_to 'Regresar al inicio', root_path %>
